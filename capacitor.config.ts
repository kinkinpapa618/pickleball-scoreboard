import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.trongtaiso.pickleball',
  appName: 'Trọng Tài Pickleball',
  webDir: 'dist/public'
};

export default config;
