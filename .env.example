# PostgreSQL Local Database Configuration Example
DATABASE_URL=postgresql://username:password@localhost:5432/database_name?sslmode=disable
PGDATABASE=pickleball
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres

# Vite API URL for frontend (Optional)
# VITE_API_URL=http://localhost:5000
